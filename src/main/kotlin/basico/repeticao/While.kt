package basico.repeticao


//fun main() {

    // criando a minha lista
    //val valores: MutableList<Double> = mutableListOf()
    //var opcao: String = ""

  /*  while (opcao != "N" && opcao != "n") {

        //Inserindo a informação pelo prompt
        println("Digite o valor deseja: ")
        val compra = readLine()!!.toDouble()

        //adicionando dentro da minha lista a compra
        valores.add(compra);

        // regra dentro do While
        println("Continuar dentro do while ? S/N")
        opcao = readLine() ?: "N"
    }

    //printando as informações da lista
    println(valores)



        // criando a minha lista
        val valores: MutableList<Double> = mutableListOf()
        var opcao: String = ""

        do {
            //Inserindo a informação pelo prompt
            println("Digite o valor deseja: ")
            val compra = readLine()!!.toDouble()

            //adicionando dentro da minha lista a compra
            valores.add(compra);

            // regra dentro do While
            println("Continuar dentro do while ? S/N")
            opcao = readLine() ?: "N"

        } while (opcao != "N" && opcao != "n")

        //printando as informações da lista
        println(valores)

        val numeros = listOf(2.4, 3.6, 5.2)

        for ((xpto, numero) in numeros.withIndex()) {
            println("posicao: ${xpto+1} - $numero")
        }

    for(i in 1..10) {
        if (i == 5){
            println("Entrou no if com continue")
            continue
        }
        println("$i")
    }
 println(nivelHierarquico (chefe = "Maria" , funcionario = "Joao"))

}

fun nivelHierarquico(funcionario : String , chefe: String){
    println ("O funcionário $funcionario reporta para $chefe")
}*/
